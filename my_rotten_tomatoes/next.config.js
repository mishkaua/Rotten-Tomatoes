/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig /* = {
    formats: ['img/avif, image/webp'],
    remotePatterns: [
        {
            protocol: 'https',
            hostname: '',
            port: '',
            pathname: ''
        },
    ],
} */
